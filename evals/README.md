# Evals

Empty until a flow works end to end against the store. Then run
`/author-commerce-evals` (commerce-builder plugin), which reads the decision record in
`CLAUDE.md` and writes the eval cases here.
